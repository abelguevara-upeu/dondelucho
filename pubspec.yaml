name: proyectolucho
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.0.0
  firebase_auth: ^5.0.0 
  cloud_firestore: ^5.4.0
  image_picker: ^0.8.4
  firebase_storage: ^12.3.4
  provider: ^6.0.0

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  assets:
    - assets/imagen_fondo.jpg
    - assets/logo_principal.jpg
    - assets/plato_pachamanca.png
    - assets/platos_asado.png
    - assets/plato_marino.png
    - assets/plato_brasa.png
    - assets/plato_postre.png
    - assets/promociones.jpg


  uses-material-design: true
